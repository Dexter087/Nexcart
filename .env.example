# Local PostgreSQL settings for running Streamlit on your laptop.
# Copy this file to .env and fill in your real password.
# Do not commit the real .env file.

DB_HOST=localhost
DB_PORT=5432
DB_NAME=nexcart_olist
DB_USER=postgres
DB_PASSWORD=PUT_YOUR_POSTGRES_PASSWORD_HERE
DB_SSLMODE=prefer
